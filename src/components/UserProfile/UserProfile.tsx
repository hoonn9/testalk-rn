import React, { useState } from "react";
import styled from "styled-components/native";

const Container = styled.View``;
const Text = styled.Text``;

interface IProp {}

const UserProfile: React.FunctionComponent<IProp> = () => {
  return (
    <Container>
      <Text>sdas</Text>
    </Container>
  );
};

export default UserProfile;
